
#DropDownList

最轻量的Android下拉菜单


添加库的方法：
1、将DropDownList.jar复制到Libs目录下；
2、修改工程属性，将jar文件加入依赖；
3、注意导入的包名为com.emercy.dropdownlist，即可使用了。

添加源码的方法：
1、用eclipse导入，如果是AS，则以Module的形式导入；
2、右键点击properties，选择Android，右下方点击add，选择DropDownList库；
3、注意导入的包名为com.emercy.dropdownlist，即可使用了。